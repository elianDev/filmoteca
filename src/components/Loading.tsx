import React from "react";
import { LoadingContainer } from "../styles/Loading.styles";

const Loading = () => {
  return (
    <LoadingContainer>
      <span></span>
    </LoadingContainer>
  );
};

export default Loading;
